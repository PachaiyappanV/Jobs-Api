import logo from "../assets/images/logo.png";

const Logo = () => {
  return (
    <img
      style={{ width: "50px" }}
      src={logo}
      alt="jobster logo"
      className="logo"
    />
  );
};
export default Logo;
